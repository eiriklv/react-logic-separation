describe("useAddReminder", () => {
  it("should call service with correct input", () => {
    // arrange
    // act
    // assert
  });

  it("should expose error when service fails", () => {
    // arrange
    // act
    // assert
  });

  it("should expose loading status while waiting calling service", () => {
    // arrange
    // act
    // assert
  });
});

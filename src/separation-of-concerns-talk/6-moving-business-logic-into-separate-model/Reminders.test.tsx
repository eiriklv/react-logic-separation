describe("Reminders (container)", () => {
  it("should render correctly while loading", () => {
    // arrange
    // act
    // assert
  });

  it("should render correctly when failed to fetch reminders", () => {
    // arrange
    // act
    // assert
  });

  it("should render correctly when failed to save reminder", () => {
    // arrange
    // act
    // assert
  });

  it("should render correctly when reminders list is empty", () => {
    // arrange
    // act
    // assert
  });

  it("should render correctly when reminders list has items", () => {
    // arrange
    // act
    // assert
  });

  it("should call handler with correct input when adding reminder", () => {
    // arrange
    // act
    // assert
  });
});

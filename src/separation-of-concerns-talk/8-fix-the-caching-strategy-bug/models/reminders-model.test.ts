describe("useRemindersModel", () => {
  it("should expose loaded reminders correctly", () => {
    // arrange
    // act
    // assert
  });

  it("should expose loading status correctly while waiting for reminders to load initially", () => {
    // arrange
    // act
    // assert
  });

  it("should expose loading status correctly while waiting for reminders to reload", () => {
    // arrange
    // act
    // assert
  });

  it("should expose error when fetching reminders fails", () => {
    // arrange
    // act
    // assert
  });

  it("should expose error when saving reminder fails", () => {
    // arrange
    // act
    // assert
  });

  it("should add reminder and refetch correctly", () => {
    // arrange
    // act
    // assert
  });
});
